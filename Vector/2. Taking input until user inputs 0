#include"bits/stdc++.h"

using namespace std;

int main()
{
    int a;
    vector<int>v;

    // Taking input
    while(cin >> a)
    {
        // When the input is 0 then it breaks
        if(a==0) break;
        v.push_back(a);
    }

    // Printing the vector
    for(int i=0; i<v.size(); i++)
        cout << v[i] << " ";
    cout << endl;
    return 0;
}
