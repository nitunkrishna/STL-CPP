#include"bits/stdc++.h"

using namespace std;

int main()
{
    vector<int>v={5,3,5,2,3,5,1};
    map<int,int>freq;
    for(int i:v)
        freq[i]++;
    for(auto i:freq)
    {
        if(i.second>0) cout << i.first << endl;
    }
    return 0;
}
